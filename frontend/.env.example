# Copie para .env.local em dev. Em producao isso normalmente nem precisa
# existir, porque o frontend e a API ficam sob o mesmo dominio (ingress
# roteando /api/* pro backend) -- ver README na raiz do projeto.
VITE_API_BASE_URL=http://localhost:8000
